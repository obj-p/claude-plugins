---
allowed-tools: Bash(bash:*)
description: Delete this session's mailbox, or all mailboxes with "all"
---

Run this and report the result:

`bash "${CLAUDE_PLUGIN_ROOT}/scripts/mail.sh" clean $ARGUMENTS`
