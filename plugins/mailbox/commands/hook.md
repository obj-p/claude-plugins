---
allowed-tools: Bash(bash:*)
description: Toggle turn-start mail injection (pass "off" to disable)
---

Run this and report the result:

`bash "${CLAUDE_PLUGIN_ROOT}/scripts/mail.sh" hook $ARGUMENTS`
